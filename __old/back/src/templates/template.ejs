<!DOCTYPE html>
<html lang="<%= data.locale %>">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= data.title || 'War of the Consortium' %></title>
  </head>
  <body>
    <%- include(`./${data.page}.ejs`, { data }) %>
  </body>
</html>